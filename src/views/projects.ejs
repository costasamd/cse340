<%- include('partials/header') %>
<main>
    <h1><%= title %></h1>
    <h2>Upcoming Service Projects</h2>
    <ul>
        <% projects.forEach(project => {%>
            <li><p><%= project.date %>: <strong><%= project.title %></strong> - <%= project.name %></p></li>
        <% }); %>
    </ul>
</main>
<%- include('partials/footer') %>    